#include <stdio.h>
int main()
{
int a, i ,f;
printf("Enter a number: \n ");
scanf("%d",&a);
if(a==0)
{ printf("Factorial of number 0 is 1 \n "); }
else
{
f=a;
for(i = a-1;i > 0;i--)
{
f =f*i;
}
printf("Factorial of number %d is %d ",a,f);
}
return 0;
}
